import { NextPageAuth } from '@/shared/types/auth.types'

const UserListPage: NextPageAuth = () => {
	return <div className={''}>UserListPage</div>
}
UserListPage.isOnlyForAdmin = true

export default UserListPage
